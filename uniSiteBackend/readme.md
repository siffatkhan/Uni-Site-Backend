

cd backend 
to open the proejct in virtaul environment 
source .venv/bin/activate

cd uniSiteBackend

to run the backend project root folder
python3 manage.py runserver



run this after each pip library install to updating requirements.txt
pip freeze > requirements.txt
